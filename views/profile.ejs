<div class="profile-container">
  <% for(user of users) {%> <% if (user.name == 'Mamta Sharma') {%>
  <h1 class="text-center">Welcome <%= user.name %></h1>
  <h3 class="text-center">
    Here are some tips to be healty after the age of 65.
  </h3>
  <div class="text-center">
    <ul>
      <h3>Activities:</h3>
      <% for(activity of user.activity) {%>

      <li><%= activity %></li>

      <%}%>
    </ul>
  </div>

  <div class="text-center">
    <ul>
      <h3>Nutritions:</h3>
      <% for(nutrition of user.nutrition) {%>

      <li><%= nutrition %></li>

      <%}%>
    </ul>
  </div>

  <div class="text-center">
    <ul>
      <h3>Vitals:</h3>
      <% for(vital of user.vitals) {%>

      <li><%= vital %></li>

      <%}%>
    </ul>
  </div>

  <% } %> <% } %>
</div>
